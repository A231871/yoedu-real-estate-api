feat(admin): [API-46] implement GDPR native SQL PII purge task and users_aud envers audit tracking

Summary of Implementation & Edge-Case Fixes:
- Flyway Migration & Audit Envers Setup:
  * Created V20260728233000__create_users_aud_table.sql for Envers user audit tracking.
  * Annotated User entity with @Audited.
- GDPR Native SQL Purge Workflow (AdminModerationServiceImpl.purgeUserGdpr):
  * Soft-delete Safety Guard: Verifies user is soft-deleted (deletedAt != null); throws BadRequestException if active to prevent accidental PII deletion of non-archived accounts.
  * L1 Cache Reversion Fix: Updates managed in-memory User entity fields before native SQL execution, preventing Hibernate dirty checking from re-flushing un-anonymized PII at commit.
  * Native SQL Execution (No Exception Swallowing): Executes native SQL queries on users_aud and users tables without suppressing exceptions, guaranteeing transaction rollback on database error.
  * Native SQL UUID Casting: Applied CAST(:userId AS uuid) for PostgreSQL driver safety.
  * No PII Leakage in Audit Logs: Audit log entry records targetUserId without storing the user's un-anonymized email.
- Controller Security & Resilience (AdminModerationController.parseAdminId):
  * Dual Principal Extraction: Supports both direct UUID string principals and email lookup fallbacks via UserRepository.
  * HTTP Status Mapping: Throws AccessDeniedException (mapped to HTTP 403 Forbidden) when context is unauthenticated.
- Summary Price Mapping:
  * Added extractCurrentPrice helper for null-safe and list-safe price extraction.
- Unit & Integration Test Suite:
  * 75 total tests run, 0 failures, 0 errors.
